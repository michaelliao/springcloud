spring:
  application:
    name: stream
  cloud:
    stream:
      bindings:
        voteInput:
          group: voteGroup
          destination: vote.exchange
          contentType: application/json
          binder: rabbitBinder
        voteLogInput:
          group: logGroup
          destination: vote.exchange
          contentType: application/json
          binder: rabbitBinder
        voteOutput:
          destination: vote.exchange
          contentType: application/json
          binder: rabbitBinder
      binders:
        rabbitBinder:
          type: rabbit
          environment:
            spring:
              rabbitmq:
                host: localhost
                port: 5672
                username: guest
                password: guest

server:
  port: 9090
