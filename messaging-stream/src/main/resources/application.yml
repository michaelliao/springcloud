spring:
  application:
    name: stream
  cloud:
    stream:
      bindings:
        voteInput:
          group: voteGroup
          destination: vote.exchange
          contentType: application/json
          binder: rabbitmq
        voteLogInput:
          group: logGroup
          destination: vote.exchange
          contentType: application/json
          binder: rabbitmq
        voteOutput:
          destination: vote.exchange
          contentType: application/json
          binder: rabbitmq
      binders:
        rabbitmq:
          type: rabbit
          environment:
            spring:
              rabbitmq:
                host: localhost
                port: 5672
                username: guest
                password: guest

server:
  port: 9090
